{
  "name": "@thecueroom/utils",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p .",
    "test": "vitest run || jest --ci || echo \"no tests\"",
    "test:watch": "vitest",
    "build": "tsc -p . || next build || vite build || echo \"no build\"",
    "lint": "eslint . --max-warnings=0"
  },
  "dependencies": {
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.3.0",
    "vitest": "^1.0.4"
  }
}
